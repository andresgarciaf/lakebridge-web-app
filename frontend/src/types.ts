export type View = 'home' | 'profiler' | 'analyzer' | 'converter'

export type EnvInfo = {
  python: string
  java: string
  databricks: string
  lakebridge: string
  host: string
  user_initials: string
}
